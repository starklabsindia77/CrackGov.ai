# Database Configuration
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL="postgresql://postgres:admin@localhost:5432/crackgov"

# NextAuth.js Configuration
# Base URL of your application (for development, use http://localhost:3000)
NEXTAUTH_URL="http://localhost:3000"
# Secret key for NextAuth.js - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="crackgov32"

# Encryption Key
# 32-byte key for encrypting API keys at rest
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY="crackgov3232"

# OpenAI API Key (Legacy - Now managed via admin panel)
# You can still set this for initial setup, but it's better to add via admin panel
OPENAI_API_KEY="your-openai-api-key-here"

# Razorpay Payment Gateway (Optional - Now managed via admin panel)
# These are optional fallback values. It's recommended to configure via Admin Panel > Payment Config
# Get these from https://dashboard.razorpay.com/app/keys
# RAZORPAY_KEY_ID="your-razorpay-key-id"
# RAZORPAY_KEY_SECRET="your-razorpay-key-secret"

# Node Environment
# Options: development, production, test
NODE_ENV="development"
